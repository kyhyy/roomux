// Package main is the roomux entry point.
//
// It will parse arguments and dispatch subcommands (e.g. starting or attaching
// to a session, listing presence, inspecting history). At this scaffolding
// stage it only prints a placeholder.
package main
